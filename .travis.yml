sudo: false
language: java
services:
  - docker
cache:
  directories:
  - "$HOME/.cache"

deploy:
  before_install:
    - docker login -u="$DOCKER_USER" -p="$DOCKER_PWD"
  provider: script
  script: "cp .travis.settings.xml $HOME/.m2/settings.xml && mvn clean deploy -Pdocker"
  skip_cleanup: true
